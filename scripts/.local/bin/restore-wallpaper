#!/usr/bin/env bash
set -euo pipefail

# restore-wallpaper
# Restores the last set wallpaper from state file, or defaults to a preset.
# Handles waiting for swww-daemon to be ready.

LOG_FILE="$HOME/.local/share/dotfiles/wallpaper.log"
mkdir -p "$(dirname "$LOG_FILE")"
exec > >(awk -v p="restore-wallpaper" '{ printf "[%s %s] %s\n", strftime("%Y-%m-%d %H:%M:%S"), p, $0; fflush() }' >>"$LOG_FILE") 2>&1

STATE_FILE="$HOME/.config/current_wallpaper"
DEFAULT_WALLPAPER="$HOME/Pictures/wallpapers/manatee-deep-dive.png"
SET_WALLPAPER_SCRIPT="$(dirname "$(readlink -f "${BASH_SOURCE[0]}")")/set-wallpaper"

trap 'echo "EXIT $? at line $LINENO (cmd: ${BASH_COMMAND})"' ERR
echo "==== run start (pid=$$) ===="

# Skip the wait entirely if swww isn't installed — set-wallpaper falls back
# to swaybg in that case, no point burning 10s on a daemon that will never
# come up.
if command -v swww >/dev/null 2>&1; then
    echo "Waiting for swww-daemon..."
    for _ in {1..20}; do
        if swww query &>/dev/null; then
            echo "swww is ready."
            break
        fi
        sleep 0.5
    done
fi

# 2. Determine which wallpaper to use
if [ -f "$STATE_FILE" ]; then
    TARGET_WALLPAPER="$(cat "$STATE_FILE")"
    if [ ! -f "$TARGET_WALLPAPER" ]; then
        echo "Saved wallpaper not found: $TARGET_WALLPAPER."
        TARGET_WALLPAPER=""
    fi
fi

# No saved state and no default available — nothing to restore.
# The daily-wallpaper timer will fetch one shortly.
if [ -z "${TARGET_WALLPAPER:-}" ]; then
    if [ -f "$DEFAULT_WALLPAPER" ]; then
        TARGET_WALLPAPER="$DEFAULT_WALLPAPER"
        echo "No saved state. Using default."
    else
        echo "No saved state and no default wallpaper; skipping restore."
        exit 0
    fi
fi

# 3. Apply it using the set-wallpaper script (which handles theming)
if [ -x "$SET_WALLPAPER_SCRIPT" ]; then
    "$SET_WALLPAPER_SCRIPT" "$TARGET_WALLPAPER"
else
    echo "Error: set-wallpaper script not found at $SET_WALLPAPER_SCRIPT"
    exit 1
fi
