# AGS (Aylur's GTK Shell)

**Status:** Archived — being replaced by Quickshell.

| Path | Status |
|------|--------|
| `ags/.config/ags/` | Empty — placeholder |
| `ags/.config/ags.v1-old/` | Legacy reference (v1 JS config) |

Legacy v1 files:
- `config.js` — minimal bar with clock + power button + calendar popup
- `style.css` — semi-transparent dark bar, glassmorphism calendar

All shell functionality moved to [[Packages/Quickshell]]. Do not add new features here.
